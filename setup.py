from setuptools import setup

setup(name='sedan',
      version='0.0.1',
      description='Lazy Batch Operations for Couchdbkit',
      author='tsuraan',
      author_email='tsuraan@gmail.com',
      url='github.com/tsuraan/sedan',
      packages=['sedan'],
      install_requires = ['couchdbkit'],
     )

